# Runtime dependencies, pinned to the versions the app was built and tested
# against. Regenerate deliberately; keep in lockstep with the conda env.
fastapi==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
sqlmodel==0.0.39
alembic==1.18.5
bcrypt==5.0.0
python-multipart==0.0.32
jinja2==3.1.6
itsdangerous==2.2.0
pandas==3.0.3
matplotlib==3.11.0
