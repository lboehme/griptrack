__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
*.db
*.sqlite3
.env
.DS_Store
*.code-workspace
.polly/
.worktrees/
.claude/settings.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Local graft + Gemini-CLI tooling — per-machine dev-environment integration,
# not project source (helper scripts contain a machine-specific path; graft/
# is regenerated locally).
.claude/helpers/
.mcp.json
.gemini/
GEMINI.md
.ignore
